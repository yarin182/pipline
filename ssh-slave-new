pipeline {
    agent any

    stages {
        stage('Build and Push Docker Image') {
            steps {
                script {
                    def imageName = "yarinct/jenkins-ssh-slave"
                    def dockerfileDir = "."
                    def dockerTag = "latest"
                    def dockerImage = docker.build("${imageName}:${dockerTag}", dockerfileDir)

                    withDockerRegistry([credentialsId: "yarinnewdockerhub", url: "https://registry.hub.docker.com"]) {
                        dockerImage.push()
                    }
                }
            }
        }
    }
}
