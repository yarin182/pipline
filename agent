pipeline {
    agent any

    stages {
        stage('Build and Run Docker Image') {
            steps {
                script {
                    docker.build('slave', '.')
                    docker.image('slave').run()
                }
            }
        }
    }
}
